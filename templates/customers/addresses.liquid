<section class="container" style="padding: 140px 0 80px;">
  <a href="{{ routes.account_url }}" style="font-size: 12px; letter-spacing: 0.18em; text-transform: uppercase; color: var(--color-cream-muted);">← Account</a>
  <h1 class="font-display" style="margin-top: 16px; font-size: clamp(32px,4vw,52px); color: var(--color-cream);">Addresses</h1>
  <p style="margin-top: 8px; color: var(--color-cream-muted);">Manage delivery addresses on file.</p>
</section>
